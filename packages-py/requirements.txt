# Minimal Python dependencies for local extractor development and validation.
# Keep this in sync with the actual packages used by the PostgreSQL-backed ETL flow.
#
# Every third-party import under packages-py must appear here. `requests` was
# imported by analysis/automated_forensics.py and analysis/forensics_benchmark.py
# and was missing, so a clean checkout following the README failed at import
# time on the analysis stage only -- after the extractors had already run.

psycopg2-binary>=2.9
pydantic>=2.0
jsonschema>=4.0
requests>=2.31
pytest>=8.0
